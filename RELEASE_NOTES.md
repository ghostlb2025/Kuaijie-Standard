# 快截标准版 v1.2.0

- 托盘新增独立的“截图保存设置”菜单。
- 保存位置支持浏览和恢复系统“图片”文件夹。
- 文件名模板支持 `{日期}`、`{时间}`、`{序号}`并实时预览。
- 同名文件自动递增，绝不覆盖。
- 自动保存、高级工具栏保存和“保存最近截图”统一使用保存设置。
- 默认高级区域截图改为 `Alt+D`，全屏截图改为 `Alt+S`。
- 马赛克像素块更加细腻，改善大颗粒感。
- 不包含 OCR、网络上传或遥测。
